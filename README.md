        DOCKER_HUB_USERNAME = credentials('docker-hub-username')  // Jenkins credential ID for Docker Hub username
        DOCKER_HUB_PASSWORD = credentials('docker-hub-password')  // Jenkins credential ID for Docker Hub password
    }
